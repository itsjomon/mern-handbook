# Practice Questions

- **Qs 1.** Create a function that prints a poem.
- **Qs 2.** Create a function to roll a dice & always display the value of the dice (1 to 6).
- **Qs 3.** Create a function that gives us the average of 3 numbers.
- **Qs 4.** Create a function that prints the multiplication table of a number.
- **Qs 5.** Create a Function that returns the sum of numbers from 1 to n.
- **Qs 6.** Create a Function that returns the concatenation of all strings in an array.

- **Qs 7.**

    ```js
    let greet = "hello";

    function chnageGreet() {
        let greet = "namaste";
        console.log(greet);
        function innerGreet() {
            console.log(greet);
        }
    }

    console.log(greet);
    changeGreet();

    /*
    Output:
    hello
    namaste
    */
    ```

- **Qs 8.** Write a JavaScript function that returns array elements larger than a number.

- **Qs 9.** Write a JavaScript function to extract unique characters from a string.

    Example: str = “abcdabcdefgggh” ans = “abcdefgh”

- **Qs 10.** Write a JavaScript function that accepts a list of country names as input and returns the longest country name as output.
    
    Example: country = ["Australia","Germany","United States of America"]
    
    Output: "United States of America"

- **Qs 11.** Write a JavaScript function to count the number of vowels in a String argument.
- **Qs 12.** Write a JavaScript function to generate a random number with in a range (start, end).